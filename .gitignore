node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test
!.env.deploy.example

# Data fetched by scripts/*.ts, deployed to gh-pages but never committed to master
/static/data/

# Locally-captured dev-only scoreboard snapshots (scripts/fetch-dev-snapshot.ts) —
# never deployed, never committed; each developer captures their own.
/static/dev-data/

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Read-only reference copy of the pre-rebuild app (Svelte 3 / SvelteKit next.360).
# Byte-identical to commit 04190e5, so it is recoverable at any time with e.g.
#   git show 04190e5:src/lib/types.ts
previous/

todo.txt
local/

# Project IDs and service account names for deploying server/ — not secrets, but no
# reason for them to live in the repo either.
server/GCP_SETUP.md
